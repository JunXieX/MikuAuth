# MikuAuth 代码审查报告

**审查时间**：2026-09-22
**审查对象**：`E:\WorkBuddy\MikuAuth`，版本 `2.5.0`（主源 43 个文件 / 9,020 行，测试 17 类 / 3,704 行，资源 `config.yml` + `messages.yml`，`pom.xml`、Actions 工作流、README）
**审查方式**：全文逐行阅读 + 并行分模块深读（数据库 / 安全与正版验证 / 配置与审计 / 交互与命令各一组）；关键结论均**单独复核**，其中配置合并的缺陷用真实代码 + 真实模板**本地实测复现**（探针见 `E:\WorkBuddy\WorkBuddy\tmp\cu-probe`）。

---

## 一、结论概要

| 级别 | 数量 | 关键词 |
|---|---|---|
| **P0 高危** | 4 | 配置文件被默认值覆盖 · 未认证即可执行管理命令 · 正版记录被非权威源删除 · 正版查询路径无准入控制 |
| **P1 中危** | 12 | 迁移不可回滚 · 静默吞异常 · 密码 72 字节 · Dialog 静默无反馈 · 审计缺位 · 性能与索引 |
| **P2 低危 / 死代码** | 14 | 路径穿越 · 跟踪条目泄漏 · 死方法与死字段 |
| **描述 / 文档错误** | 9 | 注释与实现不符 · `MikuAuth-代码理解报告.md` 已过期 |

**总体评价**：架构与工程水准明显高于同类 MC 插件——`PasswordHasher` 的常量时间比较与渐进升级、`TransferCoordinator` 的失败分流、`DatabaseManager` 的 fail-closed 迁移闸门、`ConfigUpdater` 的"只增不改 + 备份 + 回读校验"都是对的选择，且大量注释写清了"为什么"。**但有两类系统性问题值得优先处理**：

1. **"用自己的字符串模型去理解结构化格式"** —— `ConfigUpdater` 用正则当 YAML 解析器，导致 P0-1 与 P1-8；
2. **"弱信号被当成权威信号"** —— 正版验证的镜像结论与权威结论不可区分（P0-3），并且这条路径完全没有准入控制（P0-4）。

---

## 二、P0 高危

### P0-1 `ConfigUpdater` 用正则当 YAML 解析器：会把用户显式设置的值静默覆盖回默认值

**位置**：`config/ConfigUpdater.java:46`（`KEY_LINE` 正则）、`:330-346`（`collectPaths`）、`:246-264`（`locateKey`）、`:355-394`（`bodyEnd`）、`:178-184`（安全闸门 `containsAll`）

**根因**：键路径完全靠"行首正则 + 缩进 ÷ 2"推导，而不是用 YAML 解析结果。任一环节漏检某个键，该键就会被判为"用户文件里没有"，于是把模板里的默认值**追加**进去；而 `containsAll` 闸门只检查"新键能不能被解析到"，**检测不出重复键**；SnakeYAML 默认允许重复键且**后者胜** ⇒ 追加进去的默认值覆盖掉用户写的值。

**实测证据**（真实 `ConfigUpdater` + 真实 `config.yml` 模板，本地运行）：

| 用例 | 结果 |
|---|---|
| 对照：用户写 `enabled: false`（不带引号） | 未写入文件，解析出的 `premium.enabled = false` ✅ |
| 用户写 `"enabled": false`（**带引号**） | 追加了 `premium.enabled`（模板默认），解析出的 **`premium.enabled = true`** ❌ 用户的 false 被覆盖 |
| 用户文件**全部键带引号** | 整个模板被重复追加一遍，文件 9,417 → **18,756** 字符 ❌ |
| 用户文件**首行就是键**且带 UTF-8 BOM | 把 `server` 段整段重复追加（`auth-server:` 出现 2 次），解析出的 `server.auth-server = "auth"`（模板默认），**用户改的认证服名被覆盖** ❌ |

**影响**：静默改变实际生效的配置。最坏情况是 `server.auth-server` 被改回默认值 → 下次启动"认证服不存在"强校验失败 → **插件整个停用**（该服变成无登录验证）；或是 `premium.fail-closed`、`database.mariadb.*` 等安全相关开关被悄悄改回默认。用户完全看不出原因，因为文件里两行都在。
**同时这也违反了类注释写死的设计约束**（`:35-41`「只增不改：绝不修改……任何已有行」「幂等」）——文件层面确实没改已有行，但**语义层面改了**。

**触发条件**：`"key":` 引号写法、首行带 BOM（部分编辑器/格式化工具会加）、非 2 空格缩进（见 P1-8）。都是 YAML 合法输入。

**修复建议**：把"键集合"的唯一真相交给 SnakeYAML——用解析结果判断键是否存在，文本层只负责"在哪个位置插入"；并在写盘前加第二道闸门：用 `LoaderOptions.setAllowDuplicateKeys(false)` 重新解析，解析失败即放弃写入。回归测试补三个用例（引号键、BOM、4 空格缩进）。

---

### P0-2 未认证玩家可以执行 `/mikuauth` 的全部子命令（离线模式下可串成账号接管）

**位置**：`auth/AuthListener.java:44-45`（`STATIC_AUTH_COMMANDS = Set.of("mikuauth", "mauth", "mikuauth-close")`）、`auth/AuthListener.java:250-263`（未认证时放行白名单命令）、`command/AdminCommand.java:471-472`（`hasPermission` 只校验 `mikuauth.admin`）

**链路**：`AuthListener` 对未认证玩家放行 `/mikuauth`，注释写"权限由命令自身校验"；而 `AdminCommand.hasPermission` **只校验权限节点、不校验认证状态**。在离线模式（本插件的主场景）下，离线身份键由昵称推导，权限插件按 UUID 授权 ⇒ 攻击者只要**在管理员本人认证之前**用管理员的昵称连入，就拿到与管理员相同的 UUID/权限，然后：

```
/mikuauth setpassword <受害者> <攻击者设的密码>     → 直接写出新哈希，受害者账号被接管
/mikuauth deletepassword <受害者> / unbind <受害者>  → 释放昵称，可被抢注
```

账号接管**不需要受害者在线**，且这些子命令中除 `passwd`（要求玩家来源）外全部支持控制台以外的玩家执行。
**前置条件**：服主安装了按 UUID 授权的权限插件（LuckPerms-Velocity 等）。若无权限插件则无人持权，风险不成立。

**修复建议**：在 `AdminCommand` 的执行入口加认证门——未认证玩家只放行 `reload`/`accounts`/`audit`/`diagnose`（只读）与 `mikuauth-close`，写操作一律拒绝并提示改用控制台；或把这条判断直接写进各写操作分支（更不容易被后续新增子命令绕过）。

---

### P0-3 正版判定的"权威性"丢失：镜像源的 OFFLINE 会删掉正版记录，进而让昵称可被抢注

**位置**：`premium/PremiumResolution.java:11`（记录里**没有**来源权威性字段，`offline()` 来自哪个源无法区分）、`premium/PremiumService.java:245-258`（镜像仲裁返回 `mirrors.get(0)`，即某个镜像的 OFFLINE）、`auth/AuthManager.java:501-505`（`if (resolution.isOffline())` → `releaseStaleAccountAsync(username, null)`）、`database/DatabaseManager.java:269-283`（`DELETE ... WHERE nickname_lower=? AND auth_type='premium' AND (password_hash IS NULL OR password_hash='')`）

**链路**：`selectBest` 在「Mojang 返回 UNKNOWN（服务故障/超时）+ 所有启用镜像都返回 OFFLINE」时，会把镜像的 OFFLINE 当作判定返回。调用方看到的是同一个 `isOffline()==true`，于是执行"死昵称清理"——**删除该昵称的正版记录**（正版玩家首次进入时自动登记的就是 `auth_type=premium` + 无密码，正中删除条件）。OFFLINE 结果还会被**负缓存 10 分钟**（`premium.miss-ttl-minutes`）。

**影响**：在负缓存窗口内，该昵称被判为"未注册 + 离线"，任何离线客户端都可以抢注成带密码的账号；此后库里就是一条 offline 记录，`decideLoginModeAsync` 按注册类型锁定为 OFFLINE ⇒ **真号主再也无法用正版会话登录**（提示"密码错误"，只能找管理员重置）。这与注释 `AuthManager.java:501`「权威源判定该昵称已不存在」不符——**这句话在镜像仲裁路径下不成立**。

**修复建议**：`PremiumResolution` 增加 `authoritative`/`sourceKind` 字段；只在**权威源明确 OFFLINE** 时才允许清理与负缓存，镜像结论只能用于"是否需要密码"的下位决策（镜像数据本身存在滞后，尤其是刚改名/刚注册的账号）。

---

### P0-4 正版查询路径完全没有准入控制（PreLogin 阶段即可被随机昵称刷上游 API）

**位置**：`premium/PremiumService.java:87-94`（`Executors.newFixedThreadPool` ⇒ **无界队列**）、`:130-170`（`resolveAsync` 无速率/并发上限）、`auth/AuthListener.java:75-89`（每个 `PreLoginEvent` 都调用 `decideLoginModeAsync`）、`auth/AuthManager.java:262-263`（未注册昵称 → `decideForUnknownAsync`）

**对比**：密码路径有一整套准入控制（`AuthManager.java:173-177` 有界队列 64 + 同 IP 间隔 + 同账号并发去重，`:934` `tryAcquireCryptoSlot`），**正版查询路径一条都没有**。

**影响**：攻击者用随机合法昵称（3-16 位 `[A-Za-z0-9_]`，能通过 `isValidUsername` 从而绕过缓存与"离线判定"短路）批量建连：
① 每个连接产生最多 3 个外发 HTTPS 请求 → 对 Mojang/Ashcon/WPME 洪泛，触发上游限流/封禁；
② 任务进入**无界队列**堆积，内存随之增长；
③ 三个源全部超时后按 `fail-closed` 拒绝 ⇒ **正常新玩家在攻击期间全部进不来**（可用性攻击）。

**修复建议**：给正版查询加与密码路径同级的准入（同 IP 令牌桶 / 全局在途上限），线程池换成有界队列 + 拒绝策略（拒绝时按 UNKNOWN 处理并提示稍后重试）。

---

## 三、P1 中危

### P1-1 MariaDB 旧表迁移不可回滚，且原表被立即物理删除（Javadoc 承诺不成立）
`database/MariaDbBackend.java:128-135`：`RENAME TABLE miku_players TO miku_players_legacy, <新表> TO miku_players` 紧接 `DROP TABLE miku_players_legacy`；而 `DatabaseManager.java:488-490` 的 Javadoc 写「迁移在事务中执行……任何异常都保留原表不动」。MySQL/MariaDB 的 DDL 会**隐式提交**，`:529` 的 `setAutoCommit(false)` 对"INSERT + 换表"不构成事务；`:556-561` 的校验**只比对行数**（非严格模式下 `VARCHAR(16)` 截断昵称也能通过行数校验）。⇒ 一旦内容出错，数据不可恢复，且行数校验拦不住。建议：换名后保留带时间戳的备份表（校验通过再清）、校验扩展到内容维度、Javadoc 改为"MariaDB 侧 DDL 隐式提交、不可回滚"。

### P1-2 `tableExists` / `tableHasColumn` 吞掉一切 SQLException，旧库迁移可能被静默跳过
`database/DatabaseManager.java:608-626`：`catch (SQLException e) { return false; }`。启动瞬间的任何瞬时故障（SQLITE_BUSY、权限、网络）都被判成"表/列不存在" ⇒ `:493` 直接 return，迁移静默跳过，插件带着旧结构启动，之后每次登录读 `uuid` 列都抛异常（玩家只见"认证错误"，日志零散难定位）。建议只对 SQLState `42S02`/`42S22` 返回 false，其余上抛。

### P1-3 BCrypt 的 72 字节硬上限没被密码策略覆盖
`security/PasswordPolicy.java:38` 只按 `password.length()`（UTF-16 字符数）校验，`registration.max-password-length` 默认 32 且上限钳到 128；而 `PasswordHasher.hash`（`:118-119`）用的 favre BCrypt 策略是 **strict**——超过 72 字节直接抛 `IllegalArgumentException`。25 个汉字 = 75 字节就能通过策略进入哈希。后果分三处：
- 注册（`AuthManager.java:1139` 附近的 `RuntimeException` 兜底）→ 玩家看到"服务器繁忙"；
- **改密 `AuthManager.java:1248` 在 `try` 之外，方法只有 `finally`** ⇒ 异常逃出 `cryptoExecutor` 任务，**玩家收不到任何反馈**（旧密码已校验通过、新密码没生效）；
- 登录 `AuthManager.java:968` 把异常当成"密码错误" ⇒ **计入失败次数，可能触发临时封禁**，误伤合法玩家（从"截断式实现"迁移来的超长密码哈希必然踩这条）。

建议：策略层改按 **UTF-8 字节数**校验（建议 ≤71），`PasswordHasher` 内部收敛异常并提供 truncate 兼容策略，`:1248` 补 try/catch。

### P1-4 Dialog 里提交空密码 → 玩家进入"完全静默"直到超时被踢
`auth/AuthManager.java:926-932`：先 `session.dialogOpen = false`，再 `failLogin(player, session, "error.password-empty", Map.of(), false)`。而 `failLogin`（`:1462-1483`）只在 `retryDialog=true` 时才会 `exitSilent`；`display.chat` 又受静默态抑制（`display/DisplayManager.java:161-163`）⇒ **聊天被吞、错误对话框不重开、Title/BossBar 仍隐藏**，玩家点"确认登录"时密码框留空就再无任何反馈。
作者其实知道这个失败模式——`:1478-1480` 的注释写着「重开失败必须解除静默：否则……玩家将看不到任何反馈」，只是这条早退路径绕过了它。建议：空密码分支先 `display.exitSilent(player)`（或让 `failLogin` 统一先解除静默）。

### P1-5 `/mikuauth passwd` 的待输入态不随断线/超时清理
`auth/AuthManager.java:1283-1318`：`pendingPasswordInputs` **只**在 `consumePasswordInput` 里被移除；`handleDisconnect` → `cleanup`（`:1627-1645`）不碰它，超时（`:1287` 60 秒）也只在"下一条聊天到达时"才发现。⇒ 管理员执行 `passwd X` 后断线重连或去忙别的，**60 秒内任何一条聊天消息都会被当作 X 的新密码消费掉并从聊天中抹除**（达标者直接静默重置他人密码）；没人输入则条目永久驻留。建议：断线时移除，并把过期判定挪到心跳里。

### P1-6 管理员写操作基本没有审计，且审计合并窗口会吞掉"触发封禁"那条
- `auth/AuthManager.java:1339` 是全项目**唯一**的 `ADMIN_ACTION` 记录点（交互式 `passwd`）。`setpassword`、`deletepassword`、`unbind`、`unlock`、`limit`、`reload` 全部**零留痕**，与 `AuditAction.ADMIN_ACTION` 的注释（"重置/删除密码、撤销会话、解锁等"）矛盾；出事无法追责。
- `audit/AuditLogger.java:74-76` 的 60 秒合并窗口会丢掉**第 5 次**失败（也就是触发临时封禁那次，详情里带着"已触发临时封禁"），而 `/mikuauth audit` 正是被 config.yml 推荐用来排查"这个号是不是被盗了"的手段。

建议：写操作统一补 `audit.record(ADMIN_ACTION, target, ..., "管理员 X 执行 …")`；把"触发封禁"这类升级事件加入不合并白名单。

### P1-7 `--dry-run` 拼错会静默执行**真实**迁移
`command/AdminCommand.java:344`：`args.length >= 4 && "--dry-run".equalsIgnoreCase(args[3])`。写 `--dryrun` / `--dry-run=true` / 多一个尾参数都算 `false`，且**不报错、不提示用法**，管理员按文档"先试运行"却直接把数据写进 `miku_players`。建议：第 4 个参数存在但不等于 `--dry-run` 时报错中止。

### P1-8 4 空格缩进的配置文件：增量合并**永久失败**（安全闸门拦下，但不写坏）
实测：把模板缩进整体改成 4 空格后调用 `ConfigUpdater.merge`，输出
`[WARN] [配置] config.yml 的增量合并结果未通过校验，已放弃写入`，`changed=false`。
原因是 `collectPaths`/`locateKey`/`bodyEnd` 三处都用 `缩进长度 / 2` 当层级（`:338`、`:254`、`:365`），且弹栈条件只有 `stack.size() > depth`，4 空格下同一级键会被串成子级。
**影响**：这类用户的文件**永远补不进新配置项**（每次启动/重载刷一条 WARN），新功能"能用但看不见、配不了"。好在闸门阻止了写坏文件，故列 P1 而非 P0。建议：按"实际缩进栈"比较（记住上一行的真实缩进），或并入 P0-1 的统一修法。

### P1-9 登录热路径 5 次串行 DB 往返 + 审计按 IP 查询全表扫描
- `auth/AuthManager.java:247` 与 `:938` 对同一昵称查两次 `findPlayer`；`:1449` `saveSession` 与 `:1452` `recordLogin` 是两次独立任务。SQLite 只有 1 个工作线程、队列上限 512（`DatabaseManager.java:69/91-100`），突发登录叠加审计写入会触顶 `AbortPolicy`，玩家被"服务器繁忙"拒绝。
- `DatabaseManager.java:685-687` 的 `findAuditByIp` 在 `ip` 列上**没有索引**（`SqliteBackend.java:92-97`、`MariaDbBackend.java:174-175` 只建了 nickname/created 索引）⇒ 管理员每次 `audit ip <IP>` 都是全表扫描 + 排序，占满唯一工作线程。

建议：合并"记录成功登录"为一条 SQL 或复用已读到的 `StoredPlayer`；两端补 `KEY idx_audit_ip (ip, id)`。

### P1-10 IP 账号配额检查与注册不是原子的（TOCTOU）
`auth/AuthManager.java:1092` 先异步 `countAccountsByIp`，判定通过后另起任务 `:1122` `register(...)`。同 IP 并发注册可双双通过检查（MariaDB 池 6 线程更明显），绕过 `max-accounts-per-ip`。建议把判定塞进 INSERT 的同一事务/语句。

### P1-11 数据库任务失败无日志，而调用方大量 fire-and-forget
`DatabaseManager.java:839-858` 的 `catch (Throwable t) { future.completeExceptionally(t); }` 不打日志；而 `AuthManager.java:1452` `recordLogin`、`:1253`/`:1362` `clearSession`、`AdminCommand.java:175` `clearSession` 等调用点都没挂 `exceptionally`。⇒ 会话写入/登录时间/会话清理的故障**完全不可观测**（`CompletableFuture` 吞异常），与"排障日志必须带真实原因"的项目要求不符。

### P1-12 `releaseStaleAccount` 在 `expectedUuid == null` 时退化为"按昵称删除"
`DatabaseManager.java:269-283`：`expectedUuid` 只影响是否追加 `AND uuid = ?`。而唯一调用方之一（`AuthManager.java:503`）正是在最不可信的分支传 `null`。建议要求显式传 UUID，或拆成命名更危险、只在权威源结论下调用的独立方法，并在删除前后写审计。

---

## 四、P2 低危与死代码

| # | 位置 | 问题 |
|---|---|---|
| 1 | `config/MikuConfig.java` §premium-conflict-file / `audit/PremiumConflictLog.java:95` / `audit/BackendKickLog.java:91` / `SqliteBackend.java:25` | 配置里的文件名直接 `dataDirectory.resolve(name)`：绝对路径或 `../` 可越出数据目录写入任意已存在文件（`Path.resolve` 遇绝对路径直接采用）。建议 `normalize()` 后校验 `startsWith(dataDirectory)` |
| 2 | `display/DisplayManager.java:35,148-154` + `auth/AuthManager.java:1634-1641` | `tracked` 只能按 `Player` 移除；而 Velocity 的 `PlayerRegistry` 在触发 `DisconnectEvent` **之前**已 `removeFromMaps`（本机 `velocity-proxy-4.2.1-SNAPSHOT` 反编译核对：`unregisterConnection` 里先 `removeFromMaps`，事件在其后的 `fireDisconnectAndCleanup` 中发出）⇒ 断线时 `server.getPlayer(uuid)` 已为空，**跟踪条目泄漏**（含 BossBar 与占位符 Map）。建议加 `stopTracking(UUID)` |
| 3 | `display/DisplayManager.java:82-85` | `bossBarEnabled` 与 `titleEnabled` 全关时 `startTracking` 直接 return ⇒ `tracked` 为空 ⇒ `enterSilent/exitSilent` 变空操作、`isSilent` 恒 false ⇒ **"对话框打开期间不刷聊天提示"这条规则整体失效** |
| 4 | `audit/AuditLogger.java:74` | 合并判定在 `try` 之外，`MikuConfig.normalize(null)` 会 NPE（`:435-437` 无 null 保护），可逃逸到事件线程；当前调用点非 null，属潜在缺陷 |
| 5 | `database/DatabaseManager.java:245-254` | 唯一冲突靠**错误文案**判断（`contains("duplicate entry")` 等）：驱动升级/服务端本地化会失效 ⇒ DUPLICATE 被误判成 ERROR。建议改用 SQLState（23505/23000）与错误码（MariaDB 1062、SQLite 19/2067） |
| 6 | `database/DatabaseManager.java:498-509,551-553` | 迁移把整表读进 `List` 后单批 `executeBatch`：大库内存峰值高、启动线程长时间被占（`AccountMigrator` 已有分批+让出策略，两套标准） |
| 7 | `database/DatabaseManager.java:451-459,691-699` | `purgeExpiredSessions` / 审计清理是无界 `DELETE`，一次删海量行会占满唯一工作线程，期间登录任务排队直至被拒绝。建议分批 |
| 8 | `database/DatabaseManager.java:440` | `clearSession` 方法签名与语句写在同一行（全文唯一），说明被漏过格式化 |
| 9 | `premium/PremiumService.java:154-162` | single-flight 是 check-then-act：`queryApis` 在 `putIfAbsent` **之前**已提交任务，并发同昵称会重复外发且落败方的结果被丢弃，与类注释"合并为一次外部查询"不符 |
| 10 | `premium/PremiumService.java:246-249` | `selectBest` 读可被 `reload()` 整体替换的 `resolvers`，而非本次查询的快照 `active` ⇒ reload 与查询并发时莫名返回 UNKNOWN（fail-closed 拒绝） |
| 11 | `premium/PremiumService.java:192-205` | `results.put`/`firstPremium.complete`/`logger.debug` 都在 `try` 之外（只包住了 `resolve`）：`logger` 为 null 时会 NPE 并让整次聚合异常完成（测试构造器允许 null，生产不触发）。注释"子任务 catch Throwable 后落成 UNKNOWN"未完全覆盖 |
| 12 | `security/LoginThrottle.java:105-112` | `reset(ip, nickname)` 连 IP 维度一起清零 ⇒ 攻击者用**自己**的一个有效账号成功登录一次，即可清掉该 IP 的撞库计数，IP 维度保护可反复重置。建议 IP 维度只随窗口自然过期 |
| 13 | `security/UuidUtil.java:37-41` | `UUID.fromString` 是宽松实现（`1-2-3-4-5` 也会返回一个"合法但错误"的 UUID），与注释"非法一律返回 null"不符；`AccountMigrator` 用它解析外部旧库的 uuid 列，畸形值会被静默转成错误身份键 |
| 14 | `security/PasswordHasher.java:113-114` | SHA 分支拿库中原文直接比较（MD5 分支显式 `toLowerCase`），大小写十六进制的历史哈希会永远校验失败（fail-closed 无越权，但属静默迁移失败） |
| 15 | `command/AdminCommand.java:133-134,175` | 内层 future 的异常无人观察（`exceptionally` 挂在外层链上）；`clearSession` 的返回值也未观察——删除密码成功但会话没清掉时没有任何痕迹，而这正是"释放昵称"的安全前提 |
| 16 | `command/AdminCommand.java:378-390` | 迁移位置白名单是**未规范化的字符串前缀比较**：`sqlite:/srv/legacy/../../etc/x.db` 可绕过。建议 `Paths.get(x).normalize()` 后比较并显式拒绝 `..` |
| 17 | `command/AdminCommand.java:280-290` + `MikuConfig` | `auditQueryLimit` 上限 200，逐条独立发包，一次查询可把 200 条消息灌给管理员客户端（无分页/节流） |
| 18 | `command/AdminCommand.java:145-155` 等 | 用手写 `.replace()` 组装占位符，绕过 `TextUtil` 的转义承诺；当前数据源（昵称/IP）字符集受限故不可利用，但后续把玩家可控文本接到这里就会变成标签注入（尤其基岩版最怕的可点击/悬停） |

**死代码清单**（已用全仓库引用核对）：

| 位置 | 说明 |
|---|---|
| `database/DatabaseManager.java:748` `importAccount(...)` | 零调用（仅 Javadoc 里的 `{@link}` 引用），迁移实际走 `importAccountsBatch` |
| `database/DatabaseManager.java:72` `Session.nicknameLower` | 只写不读（调用方只用 `ip()`）；**待确认**（同名访问器在 `LegacyRow`/`StoredPlayer` 上另有使用） |
| `migrate/MigrationSource.java:35` `tableName()` | 零调用；SQL 里的表名硬编码在 `AccountMigrator.java:205-208` ⇒ 表名两处真相 |
| `config/ConfigUpdater.java:271` `Entry.indent` | 只写不读 |
| `config/MikuMessages.java:82-85` `prefixed(String)` 单参重载 | 零调用（34 处调用全部传了占位符 Map） |
| `command/{Login,Register,ChangePassword}Command` 的 `!(source instanceof Player)` 分支 | 三个命令的 `hasPermission` 都是 `source instanceof Player`，Velocity 在 `execute` 前先做权限判定 ⇒ 控制台分支不可达，`error.players-only` 永远不会显示（CloseDialogCommand 无此分支） |
| `database/DatabaseManager.java:52-53` `RegisterResult.DUPLICATE` 注释 | 注释说"昵称已存在（主键冲突）"，实际 UUID 主键冲突也返回它 |
| `audit/{PremiumConflictLog,BackendKickLog}.flush()` | Javadoc 写"测试与关服时使用"，实际关服只调 `shutdown()`（内部 `awaitTermination`），`flush()` 仅测试调用 |
| `migrate/AccountMigrator.java:127-130` 注释 | 声称"流式处理、全程只保留一个批次、内存峰值低"，但 `ResultSet` 未设 `setFetchSize`、URL 未开 `useCursorFetch` ⇒ 读取侧仍全量缓冲（写入侧确实分批） |
| `migrate/MigrationReport.skipped` | 文档是"账号已存在，不覆盖"，但整批写失败时也计入（`AccountMigrator.java:146-158`）⇒ 把"写库失败"说成"账号已存在" |

---

## 五、描述 / 文档错误

| # | 位置 | 问题 |
|---|---|---|
| 1 | `MikuAuth-代码理解报告.md:1,25,29,195,207` | **已过期**：标题与正文仍写 `v2.4.3`、`MikuAuthPlugin.VERSION = "2.4.3"`、`cn.miku:mikuauth:2.4.3`、`finalName = MikuAuth-2.4.3`，并声称 **`maven.compiler.release = 17`**（实际已是 25）；文件尾部虽追加了 2.5.0 章节，头部未同步 |
| 2 | `auth/AuthManager.java:501` | 注释"权威源判定该昵称已不存在"，但该分支在镜像仲裁路径下也会进入（见 P0-3） |
| 3 | `database/DatabaseManager.java:488-490` | Javadoc"迁移在事务中执行……任何异常都保留原表不动"在 MariaDB 侧不成立（DDL 隐式提交 + 立即 DROP，见 P1-1） |
| 4 | `premium/PremiumService.java:20-25,32,37` | 规则 2 的"冲突检查"只存在于慢路径，快路径（`firstPremium`）会绕过；"同昵称并发请求合并为一次外部查询"在竞态下不成立 |
| 5 | `premium/PremiumDecider.java:151-159` | 字段注释说 `purgeStale` 清的是"未被消费的残留"，实际同一个 `removeIf` 也作用在正在生效的 `verifiedUuids` 上 ⇒ 慢客户端（PreLogin→ServerConnected 超过 60s）的正版玩家会掉回密码流程 |
| 6 | `resources/config.yml:6` vs `:318-319` | 文件头说"修改后可通过 `/mikuauth reload` 热重载"，而同一文件末尾自己写着"别名修改后需要重启代理才会生效"；实测 `database.*`、`audit.premium-conflict-file`、`audit.backend-kick-file` 在 reload 后**完全不生效**（池与文件句柄在构造期建立）。建议头改为"部分配置项可热重载"并在这些键旁标注"需重启" |
| 7 | `resources/messages.yml` 的 `admin.help` 列表 | **缺 `migrate` 一行**（代码 `AdminCommand.java:38-40` 与分支都支持），管理员在游戏里看不到账号迁移功能 |
| 8 | `security/TextUtil.java:17` | 类注释"所有解析均为纯函数、无线程共享状态"，同文件 `:30-32` 另有静态 `STATIC_CACHE`（有 1024 上限、以渲染前原文为键，`reload` 后不会命中旧文本——这两点是对的，只是注释自相矛盾） |
| 9 | `resources/config.yml:292-294` / `MikuConfig.java:344-350` | `ssl-mode` 写错（如 `verify_full`）会**静默回退 `disable`**（明文连接）且无任何日志，与注释给人的"会被提示"印象不符；`display.bossbar-color/overlay` 同类静默回退 |

---

## 六、测试缺口（"改代码不会让测试变红"的地方）

1. **`ConfigUpdater` 缺三类输入**：引号键、首行 BOM、非 2 空格缩进。`ConfigUpdaterTest` 的 18 个用例覆盖了 CRLF、非法 YAML、空文件、幂等、备份、废弃键——唯独没有这三种，所以 P0-1/P1-8 全部漏网。**这正好说明"安全闸门"给了虚假的安全感**：闸门只防"写坏 YAML"，不防"语义被改"。
2. **登录模式的权威性区分没有测试**：`PremiumServiceTest` 有并发/超时/冲突用例，但没有"镜像 OFFLINE + Mojang UNKNOWN"这一组合（P0-3）。
3. **正版路径无准入控制没有测试**（也无从测试，因为功能不存在）——建议修复时补"同 IP 高频建连被限流"的用例。
4. **密码策略没有字节长度用例**（P1-3）；`PasswordPolicyTest` 只覆盖字符数。
5. **`DisplayManager` 的静默/跟踪生命周期没有测试**（P1-4、P2-2、P2-3 都落在这一块）。
6. **管理员命令的审计与权限门没有测试**（P0-2、P1-6）。

---

## 七、看着可疑、但复核后是**对的**（不要误改）

- `SqliteBackend` 单连接 + `release()` 空实现：由 `workerThreads()==1` 串行化保证，注释已说明；`SqliteBackend.swapPlayersTable` 的 `DROP+RENAME` 在 SQLite 下**受事务保护**（可回滚），与注释一致（MariaDB 侧则不然）。
- `requireSafeIdentifier` + 表名拼接：标识符是硬编码常量且有 `[a-z0-9_]{1,64}` 护栏，非注入面。
- 所有账号/审计查询都用 `PreparedStatement` 占位符，SQL 无注入。
- 正版查询的 SSRF 面闭合：endpoint 来自枚举常量，昵称先过 `isValidUsername` 再 `URLEncoder.encode`；HTTP 客户端有显式 UA、禁重定向、禁缓存、响应体 64KB 上限、只读 200 的 body。
- `isUniqueViolation` 用 `Locale.ROOT`（规避土耳其语 `i/I` 问题）——结论正确，只是判据脆弱（见 P2-5）。
- `QUEUE_CAPACITY=512` + `AbortPolicy` 是有意的有界队列设计，拒绝路径被转成 future 失败且调用方有 fallback。
- `TextUtil.escape` 的转义顺序（先 `\` 再 `<`）正确，占位符值不参与色码转换，昵称里的 `&c` 不会变成颜色。
- `DialogService` 故意没有 `clear()`，且有注释说明"发 ClearDialog 前必须确认版本，UNKNOWN 会断线"；UNKNOWN 一律降级聊天提示符合硬约束。
- 基岩版玩家不写库、不占 IP 配额是**设计**而非遗漏（注释 `AuthManager.java:696-706` 已说明副作用）。
- PacketEvents 2.13.0 的 API 用法（`CommonDialogData`、`MultiActionDialog`、`TextInputControl`、`ClientVersion.V_1_21_6/UNKNOWN`、`WrapperPlayServerShowDialog`、三个 `CHAT_*` 包类型）经 `javap` 逐一核对**全部存在**，无编译期错配。
- 离线身份键委托 `UuidUtil`（= `UUID.nameUUIDFromBytes("OfflinePlayer:"+name)`），正版身份键取 Mojang UUID；登录/改密校验都走 `PasswordHasher`（三处直连 BCrypt 都是**生成**哈希，不是校验）——三条硬性不变量总体成立。

---

## 八、建议的修复顺序

**第一批（安全 + 数据正确性，建议尽快出 Beta）**
1. P0-2 未认证玩家禁用管理写命令（改动小、收益最高）
2. P0-3 给 `PremiumResolution` 加权威性标记，非权威结论不再删除记录/不复用负缓存
3. P0-1 + P1-8 把 `ConfigUpdater` 的键集合改用 SnakeYAML 结果 + 重复键闸门（顺带修 BOM/缩进）
4. P1-1 P1-2 迁移路径：保留备份表、异常不再静默、行数校验外补内容校验
5. P1-3 密码策略补 UTF-8 字节上限 + 改密路径补 catch

**第二批（可用性 + 可观测性）**
6. P0-4 正版查询准入控制 + 有界队列
7. P1-4 P1-5 交互态收尾（空密码解除静默、passwd 待输入态随断线清理）
8. P1-6 P1-7 管理写操作补审计、`--dry-run` 拼错报错
9. P1-9 补 `idx_audit_ip`、合并登录热路径的 DB 往返
10. P1-11 数据库任务失败打日志

**第三批（清理）**
11. P2 死代码清理、注释修正、`MikuAuth-代码理解报告.md` 更新到 2.5.0（把 `release 17` 这类过期事实改掉）

> 注意：任何涉及**行为**的修复（1-10）都应遵循项目既有规则——改完交 CI 编译测试，测试数/版本号按"测试版更新"（第三位 +1、带 `-Beta`）走；README 与 Modrinth 项目页共用同一份文档，改文案只改 README 后同步。

---

## 九、本次未覆盖 / 待确认

- **未跑真实服务器验证**：全部结论来自源码阅读、本机反编译核对与本机实测（`ConfigUpdater`），未经真机联调；P0-2、P0-3、P1-4 建议在测试服复现确认后再改。
- **待确认**：`Session.nicknameLower` 是否完全无引用；Hikari 在 `validationTimeout > connectionTimeout` 时的具体回退行为（P1 相关，`MariaDbBackend.java:65-68` 与 `MikuConfig` 的 500ms 下限存在矛盾，配 500 时会告警）。
- **未做**：依赖漏洞扫描（CVE 数据库比对）、`mvn dependency:tree` 冲突分析（本地不跑 Maven）、基岩版真机兼容测试。
